# WGS / GATK CCE Step1–6 连接恢复与受限自动续跑设计

- 任务：CCE-CONNECTION-RECOVERY-20260917。
- 状态：已按用户批准进入独立分支开发；2026-09-23 用户授权补齐当日断线类型后继续 P0。实现/验收进度见同目录 plans 下的 P0 progress，不代表生产已启用。
- 设计基线：测试分支 `jiucheng/test/wgs-local-main-sync-20260917`，`e17c2ac`。
- 修订基线：上述测试分支的本地远端跟踪引用 `9333160`；隔离文档分支 `jiucheng/docs/cce-recovery-design-20260922`。
- 2026-09-22 原文档交付不包含实现；后续授权仅含独立开发与 BS10610 隔离验收，不含生产部署或真实任务恢复。

## 1. 目标与边界

### 1.0 2026-09-23 优先级修订：先交付 P0-2 底层能力

本节是原 CR-01/CR-03 的实施顺序修订，不新增第二套重试机制；与下文早期
“尚未实现/不含镜像”的表述冲突时，以本节当前状态和限定制品范围为准。

**P0-1 仅指集群只读健康检查**，不是批次恢复。2026-09-23 12:03 UTC
观测 API readyz 正常、查询可用、控制器租约持续更新且持有者稳定；未验证
新建 Job→Pod 或负载能力。后续执行仍须重新检查，不把该快照永久当成门禁。

**P0-2（亦称 P-02）包括 Master 启动/交接、Job 自动回收和 cce-pipeline
安全接回能力**，回收不是后续可选优化。先完成可由既有认证人工入口调用的
底层闭环；完整自动决策、计次、派发和下游联调继续属于
同一个 P0 后续验收，不等待全部 UI 开发，也不绕过原安全检查直接操作 helper。

#### 已有成果必须复用

2026-09-23 核对的独立分支为 `jiucheng/runtime/CR01-cce-recovery-20260922`，
HEAD `e921e3a`；插件 bs7 源提交 `25297f9`。这是研发分支/制品检查，不代表
已合入本测试分支或部署生产。已有内容包括 Worker 创建去重/接回、源头错误
候选、提交清单与 UID 探测、证据读取、预算预约、旧回调/清理及部分回执投影
保护。沿用已完成的针对性验收，不重写、不重新跑未变动用例。

尚缺可信 Master 终态与绑定生产端、实际 runtime caller、完整 adapter/派发
链和整体验收。当前 evidence-to-reservation 桥接不是可用的自动恢复入口。
实现时在独立 CR01 分支整合本修订，不能从测试分支重新复制实现一套。

#### P0-2A：Master 配置与交接合同

- 在 cce-pipeline 源码生成端检查并固定 Master manifest：digest 钉住的镜像、
  明确启动脚本、serviceAccount、挂载、工作目录；保持 backoffLimit=0 和
  restartPolicy=Never。保留既有资源与总 deadline，不以增大资源/无限延时
  代替错误处理。不升级 Snakemake 核心；插件及 Master 制品只做必要适配。
- 复用现有 MASTER_HANDOFF、recovery journal 和步骤记录，追加并绑定
  request_hash、execution_generation、Master UID、配置摘要及首次交接截止时间。
  实际字段复用已有同义字段，不平行生成第二份身份来源。
- 状态顺序为 JOB_CREATED→POD_READY→START_SENT→START_CONFIRMED。发送前
  原子保存交接意图；START_SENT 不是分析已经开始。Master 读取并校验完整
  metadata 后，在持久证据目录写入 UID/代际绑定的 START_CONFIRMED，再进入
  Snakemake；重复 START 文件不会创建第二个执行进程。
- 控制端断连后先查原 Job UID、Pod、持久确认与原 journal。已确认则进入监控；
  同一活跃 Master 等待输入则完成原交接；Pod 尚未就绪则在原截止时间内等待。
  控制端重启不重置截止时间，不因已有 Job 暂无 Pod 就另建 Master。
- 输入通过既有暂存/原子发布机制完整校验后才发送 START。START 响应丢失
  先读确认；未知状态停止重复写入，不在可能已开始后覆盖运行配置。
- 交接超时记录结构化 handoff_timeout，注明是否已确认 START；它不等于
  生信 rule 失败，也不自动加入可替换 Master 的白名单。

#### P0-2B：可信终态及原对象接回

- Master 正常成功、可捕获的启动/执行失败写入现有持久终态记录，绑定
  UID、代际、request/config 摘要、退出码、阶段、P0 根因候选与证据完整性。
  原子落盘后由 runtime 读取；SIGKILL/OOM/缺记录继续为未知，不能伪造 seal。
- runtime 查询保留结构化原因，区分网络/服务临时错误、403、404、解析失败。
  复用原有有界只读重连和总截止时间；查询错误不得被转换成对象缺失或执行失败。
- 活跃 Master 只接回；成功必须满足现有分析成功契约并校验当前 UID，单独的
  Job succeeded 或旧 RUN_FAILED 都不覆盖原生结果。确认成功后经正常回执
  路径推进下游，不手工改数据库、不把监控 task 无条件设为 success。
- 真实失败只有完整终态、身份一致、所有旧 Worker/派发进程停止后才能进入
  原 Resume 原语。创建响应丢失沿用同一 action 和确定性对象查询，不能多层
  创建重试。目录互斥保留，generation 只隔离回写，不能替代活跃执行检查。

#### P0-2C：强制 Job 自动回收及历史任务边界

- 配套设计见 [TTL 与跨 Master 续跑](../../46_JOB_TTL_CROSS_MASTER_RECOVERY_DESIGN.md)。
  必须在插件 Worker、cce-pipeline Master、现有 reader Job 生成端写入
  spec.ttlSecondsAfterFinished；WGS/GATK、CLI 和恢复创建均走统一生成端。
  拟定 TTL=100 秒，采用工程师示例作为项目初值，不宣称厂商强制该值。
  先发布可信证据与兼容 consumer，再启用模板；不能只缩短 TTL 后继续使用
  “必须查到旧 Master”的 consumer，也不能无限保留历史 Job 作为恢复方案。
- Complete/Failed 才开始 TTL；回收 Job 及其 Pod，不清理 SFS/OBS/项目证据。
  不强删 finalizer，不给历史对象批量补 TTL；活跃 Job 不适用终态 TTL。
  TTL 不等待日志/证据采集，断连跨 TTL 且无终态证据时必须停止自动恢复。
- 新契约中，Job 缺失但有可信当前终态/完整 Worker 证据时按终态分支处理；
  证据缺失、UID 冲突、Worker 清单为空或无法确认完整性时返回待确认，禁止重建。
- 旧 bundle 不原地修改；受控兼容 consumer 只使用旧格式实际能证明的内容。
  不能给历史 RUN_FAILED 补写 UID，也不能把新提交代际套到旧日志。
- 当前批次快照：D 云端 Master Complete 但平台监控失败，应优先核对当前
  原生终态后接续下游；B/E journal 与 handoff UID 不一致；C/WES B 缺当前
  终态；五批次本地 Worker 清单为空。均不是自动重建授权，也不写批次号特判。
- 需要 reader Job 刷新 SFS 证据时，使用原受控采集入口并另行授权；只读预检
  不自动创建 reader。无法补齐的历史任务保留人工决策，不降低新契约门禁。

#### P0-2D：华为云容量、AOM 和告警交付

严格落实工程师其余三项建议，详情和官方来源见配套设计第 0、5 节：

- 默认 1000 是 AP Pod 配额，包含插件占用，不是 Job 上限。汇总所有共享
  集群入口的并发、Master/Worker/reader 和留存对象，使用现有投递配置预留
  余量；同时控制终态 Job 积压，不能只统计本批次 running。
- 按官方指导上报 Prometheus 数据至指定 AOM 实例，保存数据新鲜度证据。
- 通过告警中心常用模板配置资源/事件告警及通知渠道；核对 Job 积压、Pod
  配额和 API/控制面异常覆盖。缺少托管指标由华为确认，不添加自建监控服务。
- TTL 实际回收、容量核算、AOM 和通知记录是生产闭环门禁。云端配置、费用、
  合成 Job 验证均另行授权；本轮不执行，也不得把文档更新标记为已上线。

#### 交付门禁与最小验收

P0-2E 批次锁交接必须与 P0-2C 回收同时交付，具体契约见配套设计第 4.4 节：
Airflow 管平台投递，保留跨 CLI/平台的目录防双写与进程互斥；解除对旧 Master
存在的硬依赖。现有批次锁按逻辑运行持有，执行 generation/UID 可条件接管；
禁止随 Job TTL 自动解锁、先删锁再抢占或仅凭 task 失败解锁。
旧执行全部停止且证据齐全才接管，旧 owner 不得释放新锁；历史身份不全待确认。
这不是新增锁系统，也不是华为建议取消锁。原文“锁不变”仅指复用机制，不排除
本节明确的所有权、TTL 解耦与接管修订。

新增受影响锁用例并入一次 BS10610 mock 验收：TTL 回收后可信失败可跨 Master
接管；活跃/未知执行阻止；两个恢复请求只有一个 owner；条件更新后响应丢失
或控制端重启接回原 journal；旧回调不能释放新锁；同目录 CLI/平台互斥、
同名不同目录独立；成功下游写入期间不提前释放；历史锁信息不足保持待确认。
不删除真实锁、不触发真实分析，也不重复未变动的配额测试。

1. 先补终态/binding 生产端和 Master 交接确认，再接到已有 runtime/adapters；
   与现有 P0 开发协同，禁止并行维护另一套 Master retry helper。
2. BS10610 隔离 synthetic/mock：创建响应丢失不重复创建；Pod 延迟沿用原
   截止时间；START 响应丢失/重启后接回；成功只推进下游；缺失对象有证据可
   判定、无证据停止；旧 Worker 活跃/旧 UID/部分写入均阻止替换。
3. 复用插件 bs7 及未变化 consumer 验收；新增路径一次集中验证，失败只重验
   失败用例。不新建真实 CCE canary、不测试真实批次，不跑全量回归。
4. 交付 source commit、插件 wheel hash、Master digest、cce-pipeline 和
   consumer 对应关系。P0-2 完成不等于自动恢复已完成；自动策略保持关闭。
5. 生产发布和真实恢复另行授权。先发布兼容 consumer/证据能力，再启用新
   模板 TTL；不覆盖原冻结制品。回滚不删除记录、不停止正常批次，已回收
   对象不可恢复，不能回滚到依赖这些对象存在的旧 consumer。

另覆盖三类 Job TTL 模板及恢复路径、断连超过 TTL 的证据缺失分支。
mock 通过只代表代码验收；授权后一次合成 Complete/Failed Job 回收检查
及 P0-2D 运维记录齐全，才能宣称防积压闭环完成。

来源提交 a1af96a/729564c/781877e 仅修改设计。当前用户已授权按计划实施，
实施计划见 [P0-2 开发步骤](../plans/2026-09-23-p0-2-master-handoff-ttl-implementation.md)。
后文 CR-01–05 的完整目标继续有效。取消此前“一律尚未
实现”的进度说法，但不把分片测试升级为端到端通过，也不重新开放已完成工作。

2026-09-23 实施边界澄清：MasterPod 报错示例仅用于检查错误记录完整性，
不新增批次故障专项或额外 Master audit/producer 框架。继续 CR-01–05，
此处禁止的是额外框架，不排除本节 P0-2A/B 明确要求的原有交接及终态生产端适配。
使用 runtime owner 实际交付的版本绑定合同；缺少可信完整终态仍拒绝自动
重启，不以候选文件、空日志或 BackoffLimitExceeded 推断满足恢复条件。

连接中断不等于分析失败；恢复连接不等于重新执行。

一次覆盖 WGS/GATK CCE 的上传、Master 创建、分析监控、发布、下载、
整理交付六个 runtime 步骤，以及 Airflow/后端对这些步骤的状态传递。
Step1–6 是 runtime 语义，不要求修改页面或 DAG 现有编号、名称、步骤顺序。

保留 analysis_id、业务 attempt、冻结版本/配置/目录及成功产物。
复用现有 Registry/adapter、操作认证、执行记录、generation、checkpoint、
发布记录、交付记录及 lease，不新增服务、锁体系、数据库表或业务状态枚举。

对第 3.1 节明确白名单的临时基础设施故障，允许设计
有界自动断点续跑，必要时由既有受控 Resume 原语替换已失败的 Master。
2026-09-22 首批两类、2026-09-23 新增 Lease/RPC 类型共用原有安全门禁，
不是通用失败重跑，也不增加自动恢复次数。

不包含 prepare、pending、生信参数、Local/SGE、Step7 清理、任意失败的自动
业务重跑、失败后自动日志打包、日志下载功能推广、镜像升级。
0919C 类配置样本数与 FASTQ 清单不一致、缺文件、自动查找/补传输入、改写
上传完成凭据均暂不纳入；保留现有人工纠正后续跑能力，不在本任务扩展它。
不合并测试分支的其他未发布功能；不修改冻结项目脚本以临时兼容。
生产 CLI/wheel 更新如确有需要，须随源代码修复单独列出发布对象并确认，
不能以本方案批准代替生产升级授权。

## 2. 依据与方案选择

前次只读诊断：测试任务的 Step3 因 `kubectl query failed` 退出，但查询到的
原 Master Job UID 与冻结 handoff 一致，仍为 active。该异常丢失底层 stderr，
不能事后断言一定是网络故障，也不能用错误摘要里的 current rule 推断 rule 失败。

基线源码存在以下差异：

- 两套 DAG 的只读 stage sensor 已有有限网络重试。
- GATK gate 的 Step3 子进程非零退出会直接落成 failed，先于 sensor 容错。
- WGS Step3 对 `kubectl query failed` 有按文本等待的兼容，但不足以区分
  认证/身份问题与连接故障，也不等于全链路恢复已实现。
- WGS 已有 resume_stage；后端当前明确限制 WGS CCE，GATK 不能直接调用。
- GATK SSH 派发失败、DagRun 失败投影也须与 runtime 的错误语义对齐。

选用“共用错误规则 + adapter 按原执行记录恢复”。只增加 Airflow 重试次数
不能修复底层已写入的错误终态；整步自动重跑则会重复创建/发布。
不合并两套 DAG，不建立新的连接恢复守护服务。

本轮比较的三种做法：仅加强查询重连无法恢复已退出的 Master；整个 DAG
自动重试可能重复 prepare/上传/发布；采用“查询重连 + 精确故障白名单 +
既有阶段 Resume”可复用身份与产物，因此选择第三种。失败批次号仅是需求
来源，不能作为代码判断条件；不通过本设计直接恢复任何历史真实批次。

## 3. 错误分类与重试责任

| 类别 | 决策 |
| --- | --- |
| 明确连接超时、重置、临时不可达，HTTP 408/429/5xx | 对只读查询有限重试 |
| 认证、权限、参数错误 | 不自动重试；记录控制/监控错误，不据此判定远端分析失败 |
| 身份、request hash、generation、Master UID 不匹配 | 阻止恢复；不切换对象 |
| 创建 Worker 的连接中断、Gatekeeper 超时、确切存储 RPC 故障，或配额 Lease 读取断连导致 Master 退出 | 仅满足第 3.1–3.3 节全部条件才自动断点续跑 |
| 其他权威执行失败、业务失败或执行进程异常退出 | 真实步骤失败；不自动重跑、不降级成成功 |
| 查询失败、对象消失但无可靠终态、模糊的通用报错 | 执行状态待确认；禁止推断成功/失败或重发有副作用的操作 |

只读查询规则：首次查询后最多重试六次，30 秒起指数退避，单次等待上限300秒。
每次请求须有明确超时；同时受原步骤总超时约束，不无限延长执行期限。
一次成功的权威查询结束本轮连接故障，后续正常轮询不消耗该轮重试预算。

职责边界：

- Airflow 到后端：复用已有 sensor 重试。
- runtime 到 Kubernetes/远端服务：由只读查询的直接调用层负责。
- 传输工具：保留原有内部重试，不套整步重跑。
- 证据桥只采集日志/事件；其异常不覆盖权威分析状态。

同一故障不能逐层获得新预算。runtime 报告重试耗尽后，sensor 不把它重新
当作一次尚未处理的临时 HTTP 错误。重连次数、首次错误时间、耗尽原因及
最后确认时间沿用已有步骤/操作 JSON 记录保存；进程重启或重复点击不能
静默重置同一恢复操作的预算。新的人工作业恢复操作可获得一轮新预算。
该“新预算”仅指查询预算，不重置第 3.2 节的 attempt 级自动续跑次数。

错误需包含脱敏类别、退出码/HTTP 状态及简短原因，不公开 token、配置正文、
带凭据 URL 或原始完整 stderr。如果 CCE runtime 已吞掉原因，应在其正式
源代码修复，再按原发布流程交付；不能靠上层字符串猜测，也不能改写已冻结
bundle。旧通用错误归为“未分类、待确认”，不无条件自动重试。

### 3.1 精确自动续跑候选

| 类别（拟定内部代码） | 必须同时具备的源头证据 | 不足以触发的证据 |
| --- | --- | --- |
| `worker_create_transport_interrupted`（0918A 类） | 精确属于当前 Master 的 Worker Job 创建调用；RemoteDisconnected/Connection reset/ProtocolError 中明确的传输中断；该异常是导致本次 Master 退出的原因 | 任意日志含 timeout/ProtocolError、只读查询失败、只有 BackoffLimitExceeded |
| `worker_create_admission_timeout`（0919B 类） | 同一创建调用收到 HTTP500，结构化原因指向 `mutation.gatekeeper.sh` 的 `context deadline exceeded`，并导致当前 Master 退出 | 任意 HTTP500、Gatekeeper 策略拒绝、403、配额/参数/权限错误 |
| Worker 创建存储 RPC 临时故障（0921D 类，`worker_create_storage_rpc_unavailable`） | 精确创建调用的 Kubernetes Status HTTP500/InternalError，内层明确存储 RPC Unavailable/peer reset；创建结果先按确定性名称/归属核对；确为 Master 退出根因 | 普通500、任意日志含 rpc/reset、无法确认已创建与否 |
| `HEAVY_SLOT_API_UNAVAILABLE`（0921B/C/E 中已定位的配额只读调用） | 精确 GET Lease 或按 job-name 查询当前 Worker Pod；typed errno111 ConnectionRefused 异常链；同一操作有界重连耗尽并导致 Master 退出 | 泛化 WORKER_SUBMIT_GUARD_FAILED、配额不足、403、owner/RV冲突、Lease写入响应不明、普通500 |

全部类别均须有权威 Master 失败/退出证据，且没有冲突的生信 rule 失败证据。
Lease 异常不能冒充 Worker 创建失败：必须保留操作种类、终止来源及既有
已提交 Worker 清单。保留完整 HeavySlotQuota；不跳过占用、续约或归属检查。
读取重连成功即接回；Lease 写操作不盲重放，先核对 owner/resourceVersion。
Worker 创建 RPC 响应不明确时先核对原 Job，存在则接回，UNKNOWN 不准重发。

2026-09-25 source 合同固定：producer 类别为
`WORKER_CREATE_STORAGE_RPC_UNAVAILABLE`，仅从精确 CREATE 的 ApiException
HTTP500、v1 Status/Failure/InternalError/code500 与锚定的 RPC Unavailable/
peer-reset 消息产生。持久化仅包含固定 operation=`create_namespaced_job`、
http_status=500、status_reason=`InternalError`、
transient_reason=`STORAGE_RPC_UNAVAILABLE_PEER_RESET`；不保存 API body。
inventory/backend 两处独立验证，仍需 ABSENT、重试耗尽及完整根因/终态门禁。
`mutation.gatekeeper.sh` 新识别仅覆盖相同 Status500 的 context deadline exceeded，
不含策略拒绝；保留既有 validation/check-ignore-label 识别边界。此项为隔离源码
验收，非线上启用或 Task5 冻结镜像已包含此能力的声明。
已归档的0921E配额故障定位到 release -> _finished -> list_namespaced_pod，
与B/C的 GET Lease 分别保存操作类型，不按批次特判。E后续代次还出现过旧
Worker活跃保护、Ready等待超时和原因已丢失的 kubectl 查询失败，不能把整个
批次及所有代次概括成同一种可恢复故障。
配额只读调用本地总调用最多3次，退避30/60秒，同一操作300秒总截止；关闭
该只读客户端隐式重试以保证计数。此处是插件本地预算，不叠加上面的6次
Airflow/backend sensor重试；也不新增attempt级续跑次数。写操作不盲重放。
新 executor-control-failure.json / executor-control-failure.v1 候选累计记录
配额故障；creation_state=UNKNOWN仅表示此控制观测不证明创建或终态，不准
据此认定Worker不存在。与executor-failure.json共存拒绝自动恢复。bs6制品
保持冻结；后续插件修复使用独立新提交/制品，不能复用bs6的验收结果。
0921D已归档500的Kubernetes Status.message为字符串，明确包含存储RPC
Unavailable和peer reset；仍须源头确认CREATE调用，不能仅匹配日志文本。
WES 的 Master 仍活跃而 kubectl 查询失败只恢复监控；backend 短暂不可用时
仅幂等传输名额等待可重试，不重放上传、Master创建或发布任务。
`BackoffLimitExceeded` 已纳入错误记录范围，但只表示 Job 失败达到重试限制，
不是第三类自动续跑根因。记录该条件时同时保留已观测 Master Pod 的退出码、
容器原因和重启信息；仅有这一条件而缺少源头证据仍转人工，不能推断为0918A
或0919B类。具体批次仅作完整性检查的例子，不新增专项故障恢复范围。
Master 仍活跃则只接回监控；不能因为读到一条创建错误而替换正在运行的 Master。
未分类、退出原因缺失、多重根因、源头错误已被吞掉均转人工，不猜测。
OOM、磁盘不足、缺 FASTQ、校验失败、镜像拉取失败和真实 rule 非零退出
不因同时出现网络错误而获准自动恢复。现有插件内部重试次数不能凭假设填写，
开发时核对冻结版本，只在其最终失败后作一次候选判定，不叠加 Job POST 重试。

错误证据通过现有受信 runtime/事件采集入口绑定 pipeline、analysis_id、
attempt、execution_id、generation、request hash、Master UID、调用类型及
根因类别；保存脱敏摘要和受控证据引用。新增字段为拟定合同，不代表现有实现。
浏览器文案、未经绑定的日志文本或前端请求不能提供自动恢复授权。

### 3.2 两种独立且持久的预算

以下为本轮建议的默认值，待文档审阅后在开发合同固定，不是当前线上设置：

- 查询重连仍按原规则：额外 6 次，30 秒起指数退避、单次上限 300 秒。
- 自动计算恢复：同一 `pipeline + analysis_id + attempt` 最多 **2 次**，
  第一/第二次分别等待 **60 秒 / 180 秒**；所有白名单根因共享该上限，不按新 UID、
  generation、DagRun、错误文本或服务重启重新计数。
- 第一次原始执行不计入这 2 次；续跑完成一部分 rule 后再次出现白名单故障，
  仍消耗同一 attempt 的剩余额度，不因出现新进度清零。
- 并发请求在既有事务/运行锁内预留同一恢复动作及序号；在有副作用的派发前
  持久记录。重复回调、超时重试、相同动作重放只接回该动作，不再扣次或提交。
- 原步骤总截止时间在首次执行时固定；替换 Master/换 DagRun 不重置期限。
  新执行只获得剩余时长；预算或期限耗尽后停止自动动作并明确转人工。
- 用户手动 Resume 不隐式重置自动额度；新增额度不属于本设计。手动操作仍可
  使用现有权限与断点流程，但必须与自动动作互斥，保留原恢复历史。

复用当前阶段/恢复操作 JSON 和 generation 历史保存 policy_version、
auto_recovery_count、next_retry_at、original_deadline、action_id、来源执行、
根因证据引用和结果。字段按接口开发任务落实，无新增数据库表/锁服务。
保存或读取预算失败时停止自动派发；不能把缺失字段解释为历史次数为零。
不为这项设计追溯启用已在运行/已失败的历史 attempt；启用时只对显式冻结了
该策略的新 attempt 生效，历史任务继续人工 Resume。

### 3.3 安全检查、派发与副作用边界

1. 由现有 WGS/GATK 阶段监控链在读取绑定的终态后提交恢复候选，后端既有
   recovery/action 层是唯一审批与预算所有者。observer 只提供证据，不能另开
   一套恢复循环；同一候选通过原失败执行身份及 Master UID 去重。
2. 核对启用策略、允许的根因、当前 attempt/config/release/workdir、剩余
   预算及总截止时间；暂停、取消、删除、Step7 或其他恢复动作存在时禁止派发。
   与 RUN-CONTROL 共用同一动作互斥/取消栅栏，用户停止操作优先于未派发恢复。
3. 确认旧 Master 终态、其 Pod 已退出，并完整查询本运行的 Worker Job/Pod。
   0918A 的创建响应丢失可能实际创建了 Worker，必须按原归属清单核对它。
   一次空列表不是不存在的充分证据；所有权不明、列表截断/不可读均阻止替换。
4. 同归属 Worker 仍活跃时先有限等待其自然结束，建议上限 10 分钟且受原步骤
   剩余期限约束；超限转人工。本功能不自动 kill Worker，不扩展批量清理。
   确认没有活动执行后，在派发临界点重新检查动作栅栏和身份。
5. 复用既有受限 Resume 原语，归档失败证据、记录前后 Master UID lineage，
   创建恢复 generation。只有确切拥有且已终止的旧 Master 对象可按原语替换；
   无法原子核对 UID/所有权的历史 bundle 禁用自动能力，不以同名对象代替。
6. 保留 analysis_id、attempt、冻结配置/版本/目录及成功输出，不执行 prepare、
   不消费 pending、不重新上传、不使用 forceall。Snakemake 按依赖与 incomplete
   标记执行未完成工作；某个未完成 rule 可从头重做，不承诺进程内存级续跑。
7. 创建响应再次丢失时按动作 journal、确定性对象身份、UID/handoff 查询接回，
   不立即提交第二份 Master。恢复序号只预留一次，查询失败不获得新恢复额度。
8. 旧失败 DagRun 的失败回调/lease 收尾受恢复动作栅栏限制；恢复沿用现有
   stage-resume DAG 路径，只进入当前阶段和必要下游。原阶段取得权威成功后
   才自动继续 Step4–6，不要求操作员逐阶段改状态。

延时/等待由现有 Airflow 编排调度持久动作实现，不新增定时守护服务；不能
把有副作用的 task 整体设为 retries=2。轮询耗尽、进程重启和回调丢失后，
现有编排读取持久动作接回；无法核实派发结果则转待确认，不能重建随机动作。

## 4. 各步骤恢复规则

| runtime 步骤 | 接回及续跑行为 |
| --- | --- |
| Step1 上传 | 原执行活跃则接回监控；确认进程退出后，用户续跑才复用 checkpoint 和成功对象补传 |
| Step2 创建 Master | 丢失响应先按原请求查询；已有 Master 则接回。只有明确原提交未发生且不存在在途创建，才允许同一请求重发 |
| Step3 分析监控 | 活跃 Master 仅重连；真实失败保留历史，精确白名单且全部门禁通过才按第 3.3 节自动断点续跑；其他失败转人工 |
| Step4 发布 | SSH 超时/响应丢失先查询原执行及发布记录；仍进行则接回、完成则确认成功；仅证明未启动且无在途派发时按同一动作安全再派发，否则待确认 |
| Step5 下载 | 原下载仍活跃则接回；确认退出后，用户续跑复用 checkpoint 和已验证文件 |
| Step6 整理交付 | 接回原进程，或依据已有 journal/交付记录续做未完成部分；不重新下载，不覆盖已交付结果 |

Step2 的一次空查询不充分证明创建请求未到达；必须与已有派发/执行记录
一起确认，依靠原任务确定性身份与服务端防重复创建能力，不新增随机 Master。
Step4 同样不能将“未拿到响应”等价为“发布未发生”。
0918A 的 Step4 120 秒 SSH 调用超时属于此处“结果不确定”，不是握手失败。
将其纳入原操作核对，不额外创建新的发布身份，也不重复已成功的 Step3。
同一 Step4 动作证明未发出后的重派发最多 2 次，复用持久序号、60/180 秒
等待与原截止时间；这是派发恢复预算，不消耗也不重置 Master 计算恢复预算。

只做必要身份、活跃执行和恢复记录检查；不全量扫描输出、计算历史文件哈希
或重新验证整个 release。现有结果允许存在。同任务旧锁按现有归属和活跃性
处理；其他任务持锁或无法确认活跃性则停止，不强行解锁。

传输 lease 不因查询失败、Airflow task 失败或超时直接释放。只有现有权威
终态/归属条件满足才能释放，避免原上传/下载仍运行时引入第二份执行。
本方案不改变 lease 数量、池配置或并发限额。

## 5. Airflow、后端和页面状态契约

区分执行结果与监控健康度，复用 `monitoring_health`、`message` 和原步骤
JSON/执行记录，不新增整套业务状态。通用失败不能直接冒充执行终态 receipt。

| 场景 | 调度与展示 |
| --- | --- |
| 有限重连中 | 保留最后确认的执行状态/进度；显示“连接中断，正在重连”及最后确认时间 |
| 重试耗尽或控制错误 | 停止自动查询；显示“监控中断，执行状态待确认”和原因，提供人工恢复入口 |
| 重连查到原执行仍活跃 | 接回正常监控，不重新派发，不增加业务 attempt |
| 重连取得权威成功 | 同步原步骤成功，放行必要下游 |
| 白名单失败，已受理自动恢复 | 保留失败执行历史，显示“自动续跑等待中（1/2）”及原因/下次时间；未启动前不声称分析运行 |
| 替换 Master 已确认开始 | 显示“自动续跑中（1/2）”；绑定新 generation，保留原 attempt 和断点 |
| 自动恢复耗尽/安全检查未通过 | 显示“自动续跑停止，需人工处理”；真实失败与执行状态待确认分别展示，不统一伪装为 running |
| 取得其他权威失败 | 明确步骤失败，保留原失败原因与断点 |
| 权威分析成功但日志采集异常 | 分析成功，监控 degraded，提示“分析完成，日志采集异常” |

Airflow task/DagRun 可因监控耗尽或控制错误结束为 failed，但后端不得仅据此
将远端分析投影为失败。失败回调、周期状态同步、runtime receipt 导入必须
遵守同一规则；不能只改页面文案而保留后端错误终态。

耗尽后的最后 running 是“最后确认状态”，不能继续对外宣称实时正常运行。
Tracker 和详情使用同一监控健康度/时间字段，进度冻结并明确过期，不继续
凭时间推进预估进度。无权威成功不启动下游；不能将监控 task 标成 success
来绕过依赖。身份校验失败也不能伪装成成功。
恢复动作状态使用现有操作字段和展示文案，不新增业务状态枚举。Tracker/详情
读取同一最新动作及当前 generation；旧 Master failed 仍作为历史保存，不能
覆盖新执行，也不能被改写为 success。剩余 rule 统计必须说明续跑分母口径，
等待时不增长百分比。恢复后仍由正常阶段回执推进，不增加手工改库步骤。

## 6. 恢复入口与幂等

复用运行操作面板和认证，通过 adapter 补齐 GATK 的阶段恢复能力。共用动作
语义和操作记录，不将 WGS 的所有运行时逻辑直接套到 GATK，也不走会增加
业务 attempt 并重新 prepare 的旧 resume 路径。

人工点击后的先后顺序：

1. 验证当前任务/attempt/冻结目录与配置身份。
2. 查询原执行及已有恢复操作；重复点击返回同一操作。
3. 原执行活跃：仅恢复监控；原步骤成功：同步状态并继续必要下游。
4. 进程确认退出：当前人工操作授权在该步骤原有断点能力内续跑。
5. 无法确认活跃性：保持待确认，不启动第二份。

只读查询本身不增加 generation；确需新监控进程或步骤执行时，用现有
generation/retry 记录留痕，明确绑定原远端执行。原执行的可信终态只能经
当前恢复操作验证后归入当前状态，旧失败事件不能直接覆盖新执行。
清除的是新执行的旧终止时间，历史记录不删除。

重开范围限当前步骤/监控和必要下游，已成功前置步骤不变。保留原生信
Master 失败后的人工续跑机制；新增自动入口仅适用第 3.1 节白名单根因，且与
人工入口共享预算记录、身份校验及动作互斥，不提供无条件的自动 Master 替换。
界面复用现有控件，按情况显示“恢复监控”或“续跑”，不新增独立恢复页面。

## 7. 预期代码与文档边界（待开发）

- `scripts/wgs_runtime_gate.py`、`scripts/gatk_runtime_gate.py`：分类/重连、
  执行结果与监控结果分离；复用既有 evidence bridge 和续跑原语。
- `dags/bio_wgs.py`、`dags/bio_gatk.py`：只读重试责任、派发结果确认、失败回调，
  不改阶段顺序/资源限额，不将副作用任务整体设为自动重试。
- 后端 WGS/GATK stage/runtime/observer/action adapter：统一投影和幂等恢复；
  加入单一候选决策、持久预算及停止栅栏，不让另一条同步路径重写旧失败状态。
- `backend/app/wgs_resume_service.py`、`scripts/wgs_resume.py` 和 GATK 对应
  adapter/helper：复用阶段恢复和 lineage，补齐自动调用门禁；不得按运行时名称
  拼一个绕过人工入口安全检查的后门。
- 现有 Tracker/详情/操作面板：复用健康度、最后确认时间和恢复操作。
- CCE runtime 所属源码：仅在源头确实丢失错误分类时补齐对应查询/Worker
  创建最终错误契约；不将本任务扩展为所有 Kubernetes 写操作重试。
  不顺带升级其他依赖；具体仓库/版本和发布制品须在实施前确定。
- 同步 API、runtime、DAG、必要前端说明，以及任务/状态/交接记录。

上述为允许修改的职责范围，不是要求每个文件都修改。开发前核对当时最新
测试/main 差异，只保留必要改动；遇到须改镜像、数据库 schema 或扩大权限
才能实现的情况，先停下向用户确认。

## 8. 最小验收与交付

仅 BS10610 隔离环境，使用 synthetic/mock 故障和原有测试工具，不提交真实
分析、不跑全量回归、不重建无关镜像。先复现，再运行受影响测试文件一次。

| 针对性用例 | 预期 |
| --- | --- |
| 查询断连后恢复 | 有限重试，原执行不重复启动，正常接回 |
| 创建/发布响应丢失 | 先确认原操作，不重复 Master/发布 |
| 重试耗尽及跨层传递 | 不重复获得预算，Airflow 失败不误写分析失败 |
| 认证错误/身份变化/非白名单真实失败 | 停止自动恢复；真实失败仍失败 |
| 上传/下载/交付接回或续做 | 复用成功产物，活跃 lease 不误释放，不进入 prepare/前置成功步骤 |
| 重复点击/旧回执到达 | 同一恢复操作，无重复执行、时间回退或旧状态覆盖 |
| 0918A 类 Worker 创建传输断连 | Master 已失败且旧 Worker 全退出时，同 attempt 自动 Resume；活跃/未知 Worker 阻止替换 |
| 0919B 类 Gatekeeper 创建超时 | 绑定的 HTTP500 + webhook deadline 原因允许恢复；普通500、策略拒绝及混合 rule 错误不允许 |
| Lease GET断连、创建RPC500 | 精确源头分类和原操作核对；普通500/写入不明/配额权限错误拒绝；Master活跃不替换 |
| WES等待传输名额时backend中断 | 同attempt/transfer_id重连，响应丢失后接回原名额；不新增传输，不抢占他人名额 |
| 两次恢复后再失败、进程重启、根因切换 | 全部白名单共享上限2，不重置次数/原截止时间，第三次不派发 |
| 自动与人工 Resume/暂停/删除并发 | 同一事务/动作栅栏只产生一个派发；用户停止优先 |
| 替换创建成功但响应丢失、旧终态迟到 | 按既有动作接回唯一新 Master，保留 UID lineage，旧执行不覆盖新执行 |
| 0919C 类12个输入仅8个 | 非重试候选；不补文件、不修改清单/完成凭据、不自动开始分析 |
| 自动恢复后原生计算成功 | 正常放行必要下游并更新 Tracker/详情，无人工改状态 |

对 WGS/GATK 参数化覆盖差异路径，不复制两套全量测试。未来开发交付报告应
逐项说明实测范围和未覆盖边界。20260917S 的真实接回另行确认，不作隐含测试。

按依赖实施：错误分类/runtime → Airflow/后端恢复与投影 → 共用展示及测试。
这是一个整体修复目标，不是三个可各自宣称端到端完成的独立补丁。
先提交测试分支代码与 BS10610 结果；main/生产合入、96 发布、运行中任务恢复
须单独授权。回滚仅还原代码/制品，不修改任务结果、数据库记录或分析文件。
新增自动策略默认关闭，按 adapter 能力与新 attempt 冻结策略启用；WGS 或
GATK 任一路径未验收不得因另一路径通过而开放。关闭策略阻止新自动动作，
已开始的计算保留监控，不终止；等待动作停止派发并展示需人工处理，历史与
预算不清空。不把代码回滚当作数据回滚或自动 Resume。

### 8.1 待开发任务卡（范围与顺序，不是代码已交付）

| 任务 | 责任及依赖 | 最小交付/验收 |
| --- | --- | --- |
| CR-01 错误证据合同 | Runtime；先核对 WGS/GATK 当前冻结版本 | 分类只读故障与第3.1节精确终止根因；synthetic 正/反例，缺失或冲突证据拒绝；必要制品适配另行确认 |
| CR-02 自动决策与预算 | Backend；依赖 CR-01 | 复用 recovery/action JSON、事务和身份；同 attempt 两次预算、同动作去重、截止时间及用户停止优先；并发/重启一次参数化测试 |
| CR-03 安全续跑及 DAG 接回 | Runtime + Airflow；依赖 CR-02 和可用的旧 Worker/UID 安全原语 | 对齐两个 adapter 的 Resume、旧工作负载静止检查、创建结果核对、回调/lease栅栏、Step4派发核对和后续阶段自动推进；只测受影响路径 |
| CR-04 统一展示 | Backend + Frontend；依赖 CR-02/03 合同 | 原有 Tracker/详情显示等待、续跑、耗尽/待确认；旧回执不覆盖新执行，不添加页面 |
| CR-05 合并验收及发布清单 | QA/发布负责人；依赖 CR-01–04 | BS10610一次匹配源码的synthetic矩阵，复用已通过且未改动的用例；列出未支持bundle及回滚，生产启用另行授权 |

复用现有 `scripts/tests/test_wgs_resume.py`、`test_gatk_resume.py`、两套
runtime gate 测试、`backend/tests/test_wgs_resume_stage.py` 及既有 DAG/UI
测试，按实际改动选择；不为了文档验收运行它们。CR-03 若现有原语不能证明
旧 Worker 已静止或无法与 RUN-CONTROL 共用栅栏，先补齐该必要依赖并复核，
不能绕过安全检查，也不顺带实施整个暂停/删除功能。

本任务卡自身不授权发布；2026-09-22/23 用户后续授权的实现记录见 P0 progress。

## 9. 本次文档交付记录

2026-09-17：用户确认上述整体范围，要求先形成文档并提交测试分支待开发。
本次仅此设计及 CURRENT_STATE/TASKS/HANDOFF 四份 Markdown 文档；不实施。
文档检查包括 diff 空白检查、链接/任务编号及范围一致性、提交文件白名单。
运行时测试不执行，因为没有应用代码、DAG、环境或数据改动。

2026-09-22：用户明确纳入0918A创建Worker断连、0919B准入超时的自动断点
续跑，并暂不纳入0919C输入修复。修订错误白名单、两个层级的预算、静止检查、
同attempt恢复、Step4派发核对、状态展示及CR-01–05；同步任务/状态/交接。
只做文档一致性、链接和差异检查，不开发、不连接远端、不合入main/生产、不部署。
