# CCE 0.8.5 Release Implementation Plan

> For agentic workers: use superpowers:subagent-driven-development, scoped tests only.

Goal: deliver reviewed cce-pipeline 0.8.5 wheel and compatible Airflow admin API.
Architecture: reuse asset transaction and existing multi-release YAML catalog.
Tech Stack: Python stdlib/PyYAML, FastAPI, unittest/pytest, setuptools.
Spec: ../specs/2026-09-15-cce-085-release.md

## Global Constraints

- Version is 0.8.5; default inactive, retain old assets and frozen attempts.
- No production deploy/install/activation, no prepare/pending/local/SGE/gate edits.
- Runtime tests/build only BS10610 WGS_test, no /tmp/home artifacts or downloads.
- Use fixed server Git /sg2/33.chenjiucheng/software/miniforge3/bin/git after verification.
- D drive cce-source is editing mirror only; cce Git remains server-side.
- Only focused tests and one synthetic cross-component smoke; no clinical runs.
- Shared wire contract is the linked spec; no duplicate asset engine or catalog.

### Task 1: CCE 0.8.5 producer

Files: src/cce_pipeline/release.py (new), cli.py, assets/cce_delivery.py,
pyproject.toml, README.md, docs/operations/cce-release.md (new),
tests/test_release.py (new), tests/test_materialize_results.py.
Read the spec for exact wire/CLI semantics. Work editing mirror
D:/pipeline/task-artifacts/cce-release-20260915/cce-source; real Git branch
jiucheng/cce-release-085 at BS:/bi/biodevrwbi/33.chenjiucheng/project/worktrees/huawei-cloud-runtime-bioinfo-permissions-083,
base ee2a81526e8914f43243f4d07e230e7d6b085330. No AGENTS found in target repo.
BS10610 preflight: server10610 chenjc:bioinfo; WGS Python has setuptools84/wheel/yaml.
Task runtime root /mnt/biodevrwsg2/33.chenjiucheng/WGS_test/cce-evidence/cce-release-085-20260915/cce.
Use apply_patch locally then exact transfers; do not test on node005.

- [ ] RED: foreign unrelated cache chmod raises PermissionError while committed
  result resume should pass; new CLI/module is initially missing. Use literal
  expected receipt fields and real fixture contracts; mock only kubectl/HTTP boundary.
- [ ] Implement narrow Step6 normalize only journal top_levels and marker, preserving
  modes and journal safety; retain rc1 terminal logs unchanged.
- [ ] Implement release.py orchestration/payload validation/stdlib HTTP client and CLI.
  Validate catalog field crossbinding before any cloud side effects. Reject replace
  and delete; validate SOURCE_READY; status-first wait then apply only NOT_PUBLISHED;
  unknown/failed status fails closed. Bound wait, allow registration-only retry.
- [ ] GREEN: focused new release tests plus materialize and existing asset CLI tests;
  TMPDIR must be a task-specific created directory under task root. No full198 suite.
- [ ] Bump version/docs, exact scoped remote Git commit after diff check; report SHA,
  RED/GREEN commands/output, changed files, uncertainties. No build until reviewed.

### Task 2: Airflow consumer

Files: backend/app/wgs_release_management.py (new), wgs_release_catalog.py only if
needed for reusable validation, main.py route wiring, config.py opt-in setting,
backend/tests/test_wgs_release_management.py (new), docs/05_API_CONTRACT.md,
backend/app/wgs_platform_service.py (CCE recovery pin only),
docs/02_ENGINEERING_SPEC.md, docs/11_DEPLOYMENT_RUNBOOK.md, CURRENT_STATE.md,
TASKS.md, HANDOFF.md. Work D:/pipeline/airflow-demo-worktrees/cce-release-085,
branch jiucheng/backend/cce-release-085, base33598aa; owns no recovery dirty files.
Read AGENTS/docs34/current/tasks/latest handoff before editing. No DB changes.
Consumes exact cce-release.v1 from spec, can test without installing cce package.

- [ ] RED: endpoint registration not present; synthetic fixture ties source/profile
  and asset hashes with literal values; candidate leaves current unchanged.
- [ ] Implement validated locked atomic catalog writes, exact replay/conflict,
  explicit activation CAS and read-only listing. Preserve historical entries and
  metadata. Authentication required even legacy auth disabled behavior elsewhere.
- [ ] In _refresh_recovery_release, CCE validates recorded pipeline_release_id
  through catalog.by_id and returns {} without changing params; missing/unknown ID
  fails closed. Leave local/SGE branch unchanged. Test frozen CCE params after a
  current-release switch, missing pin rejection and unchanged local behavior.
- [ ] GREEN: focused tests for admin/disabled/CSRF, binding/digest mismatch, identical
  replay/changed conflict, current unchanged, CAS activation and old by_id lookup.
  Run on BS10610 isolated cached backend image with --network none, not live container.
- [ ] Update contracts and dated state handoff, scoped Git commit. Report evidence.

### Task 3: Integration and wheel

Consumes reviewed Task1/2 commits. No business implementation changes planned.
- [ ] Run one synthetic producer receipt to real Airflow TestClient integration on
  BS10610 cached isolated environment. Assert register inactive, explicit activate,
  old release retained; no cloud or production DB calls.
- [ ] Build0.8.5 using clean remote source with existing WGS Python, no dependencies
  downloaded; isolate wheel installation under task root and inspect metadata,
  CLI help, Step6 asset, terminal logs asset. SHA256 wheel and transfer local.
- [ ] Write exact two-commit provenance/test manifest and update component release
  notes; final concise report distinguishes developed/built from deployed/active.
