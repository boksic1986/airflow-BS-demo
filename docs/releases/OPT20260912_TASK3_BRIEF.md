# Task3 implementation brief — resources

Binding Global Constraints and Task3 of ../superpowers/plans/2026-09-12-platform-optimization.md; read OPT20260912_RESOURCE_RESEARCH.md for official API and current test findings.

Clarification communicated to user: existing quota actually uses work_job/work_pod, one grouped workload consumes one lease. Preserve executor semantics; do not pretend per-rule enforcement or change/rebuild Master. Expose unit and correct counts. Root saw25 complete Leases/11 holders with read-only kubectl; waiting source missing. Occupancy may remain known even when waiting unavailable, with per-field freshness/reason and available-compatibility fields. Count reserved holders even old; never reclaim in monitor. No synthetic0, no infer limit from frontend config.

Fix missing test global snapshot with explicit test-configured launcher/collector output (no production hardcoding). t640 UID6801 test private root /home/ctapa/.config/airflow-wgs-test and runtime evidence /sg2/biodevrwsg2/33.chenjiucheng/WGS_test/airflow-ctapa/wgs-runtime/cce-evidence. Existing test runtime.env references CCE_OPERATOR_CONFIG=/home/ctapa/.config/wgs/cce.yaml. Private config stays in place, no credential copy. Do not modify production launcher/process. Root will deploy tested task code/launcher after review; implementation agent no launch/deploy.

Metrics: Dashboard Cloud Resources must independently show heavy status even if SFS missing. Runtime metrics and resource package balances separate. SFS read/write/total exists; preserve source label and no missing-as0. CPU/memory realtime only actual cloud metrics, not local node values or balance hours. BSS collector separate credentials/domainGlobalCredentials, fixed HTTPS global BSS endpoint (no arbitrary credential exfiltration host), hourly cache. Complete bounded pagination/batches, units from dictionary, Decimal validation, per quota cycle/expiry display, no summing different units. Dedicated private credential permissions; absent configuration reports not_configured with reason. No live BSS request is authorized without correct dedicated read-only identity; synthetic signed transport tests sufficient and deploy unavailable state honestly.

Backend spool/API do not expose order IDs/resource IDs/account IDs/credentials/raw cloud errors; use opaque stable keys/safe codes. Preserve last-good snapshot with stale/error indication on403/429/timeouts and incomplete results. No new DB table necessary: existing resource snapshot JSON or dedicated validated numeric spool; frontend must not poll BSS. Additive API/types and script/env docs. Avoid duplicating standalone heavy logic; ensure deployed module and collector source validation agree.

Own resource scripts/backend metrics/API projection/DashboardResourcePanels types/tests; don't edit submission/rule/QC/timing. Use separate remote test source under /mnt/biodevrwsg2/33.chenjiucheng/WGS_test/cce-evidence/OPT20260912-resources, cached backend and Node builder (see task2 brief). TDD onBS10610 only, no local runtime tests/network installs. Report docs/releases/OPT20260912_RESOURCES_REPORT.md with red/green results, files, explicit not-proven enforcement and missing billing privilege. No subagents.

Carry-forward: Task1 scoped frontend tests pass, but existing WgsProductionUi.test.tsx test "replaces duplicate dashboard metrics with actionable attention and total throughput" fails on the baseline. Review its fixture/layout assertions when changing Dashboard resources and repair the actual mismatch rather than skip it. The other known legacy run-detail request-count assertion belongs Task2. Final integrated UI suite must distinguish any remaining unrelated failures.

Fresh dependency inventory (read-only): deployed backend Python lacks requests and huaweicloudsdkcore/bss; t640 nipttest has requests and huaweicloudsdkcore but no huaweicloudsdkbss.v2 bindings. Prefer standalone numeric-spool collector using existing core authentication on the node, with backend only validating/reading spool; don't introduce eager SDK imports into backend or install network packages. Transport can be injected for synthetic tests. Existing Cloud Eye follows this separated collection pattern. No BSS credential or live query is presently configured/authorized.
