# Local/SGE R2 — BS10610 test acceptance, 2026-09-16

User authorized final bounded acceptance and test deployment, not main merge or
BS96 release. User subsequently confirmed browser retest successful. This is
monitoring integration acceptance, not proof of real Local/SGE analysis execution.

## Source and services

- Host: SSH BS10610, server10610, chenjc. Control root:
  `/mnt/biodevrwbi/33.chenjiucheng/project/airflow-WGS`.
- Release: `releases/20260916-onprem-review`. Source basis deployed359df11 plus
  e583833 main GATK fix and current R2 source through isolated Git index; no
  branch/ref merge. Feature branch remains uncommitted.
- Private controls: `candidates/onprem-review-control/compose.json` and
  `rollback.json`. These contain runtime secrets; never publish their contents.
- Backend e95d75c0695b and observer75bca7473155 mount release/backend at /app.
- Airflow API fc89ad88c70e, scheduler f4c89955a36b, worker7adcb7a790a9 add the
  native monitor DAG mount/flag; original WGS/GATK/shared DAG mounts retained.
- Frontend346c262603d8 retains nginx image, mounts release/frontend-dist.
  Served JS index-C3XNguqb.js and CSS index-BFPGoplr.css verified.
- PostgreSQL6cf33066b97a, Redis ae5b4e30caf0, node probe f8d3f7b2311b and
  metrics collector163cfcd5e5af unchanged. Current symlink remains old
  releases/20260912-opt-4d3d24e6; rely on actual mounts.
- Schema additive0024 ->0025 ->0026; no row deletion. Scanner and auto-dispatch
  both false. Original real execution gates/images/profiles unchanged.

Native registration/claim/monitor enabled only on test, instance
bs10610-onprem-review. Project roots are the test root
`/sg2/50.ctapa/project/HWcloud/WGS_test/onprem-review` and control candidate
`candidates/onprem-review-control/projects`. /sg2 is read-only on this host;
no clinical directory/mount permissions changed. Synthetic materials use the
candidate root. Private snapshots owned6801:520 mode700 map to
`/data/onprem-snapshots`; control ACL permits backend traversal only.

## Verification and evidence

Cached containers, no Docker Hub pulls or complete workflow reruns:

- `pytest tests/test_wgs_onprem_views.py tests/test_wgs_onprem_controller_joint.py tests/test_run_batch_display.py -q -p no:cacheprovider --tb=short`:6 passed3.13s.
- `vitest run src/features/run-detail/NativeExecutionPanel.test.tsx src/components/LogViewer.test.tsx`:5 passed2.84s; npm production build passed.
- `/api/health`, `/api/health/db`:ok; native DAG imported, two monitor executions
  succeeded. No direct status editing.
- Authenticated synthetic first/resume uses one analysis_id
  `WGS_20260915_171640_A550DB`, attempt1, generations1/2; executions
  wse_d7c734da848675ced0afd5f1 and wse_7dce0d6aef85f76d2206983d.
  Controller Step1 is synthetic exit0, not real Snakemake/FASTQ/prepare/qsub.
- Idempotent registration, duplicate claim409, current count1, distinct frozen
  samples/configs, rules/log search and inherited-then-replaced latest QC passed.
- Evidence: release/live-acceptance.log, synthetic project/acceptance-summary.json,
  release/source-sha256.json. Three original successful runs retained, plus one
  synthetic run. Synthetic account disabled/sessions expired; temporary private
  credential file removed and absence verified.
- Browser automation lost connection; user independently checked and confirmed
  success. [Review run](http://172.17.106.10:12959/runs/WGS_20260915_171640_A550DB).
- WGS owner HTTP/HTTPS-only URL-gate patch34b487d verified at candidate
  `/mnt/biodevrwbi/33.chenjiucheng/project/wgs-4.2.0`; owner URL tests9 passed,
  8 deselected. User permits HTTP; private credential/no-redirect rules remain.

Final wiring fixes: current native configured-scope list count; skip default cloud
phases; forward native match_index while retaining main continuous log context.
No original local/SGE selection, pending handoff or bioinformatics rules changed.

## Bounded issues resolved / rollback

Initial /sg2 directory creation failed EROFS, so synthetic project used approved
candidate area; no remount. SSH gateway18 interrupted an attempt; read-only host
check recovered. Packaging RunDetail conflict resolved preserving main log search.
Initial acceptance container mount was read-only; corrected private disposable
acceptance composition only. Tests exposed count/match navigation gaps, fixed
before final acceptance. No expanded real compute/multi-account tests performed.

User explicitly requested deleting ONLY the newly generated DB dump
`candidates/onprem-review-control/biodemo-before-0025-0026.dump` (775894 bytes).
Deletion confirmed; no replacement backup. Existing records, results and pending
not removed. Thus no dump-based rollback is available. Restore six-service code
composition from private rollback.json if required, retaining additive schema,
snapshots and evidence. Do not downgrade DB or silently delete records.

Next: await explicit integration/release direction; do not merge/push main or
deploy BS96 automatically after this test acceptance.
