# Selective deployed-code synchronization

User requested completed Airflow development on main and production, then push.
Base:29ade686241d37ec37c01ab6b45a2985371e0d59 (both remote branches).
No service deployment or restart is part of this operation.

## Included

- Exact BS96 original-file reference API/model/worker/readers, minimal main/models
  hooks and additive0022/0023/0024 migration chain. No credentials or registration.
- Samples read-only ledger/API client and shared compact button styles.
- Already deployed GATK7.6.X/7.7.X configuration resolution and compact resources.
- Existing synthetic consumer/config tests and corresponding deployment/API/docs.

Source verified from /data/airflow-WGS/releases/20260914-ledger-c2e491c;
backendc787bfd06e5b/frontend60049e7897bc. Original selection behavior retained.
The dormant cloud/journal reader modules are existing deployed loader dependencies;
only wgs_files is approved. No new protocol activation.0022 is retained for migration
continuity, not promotion of the experimental attempt-scope replacement.

## Excluded / preserved in development

Do not merge the whole79f6f01 development ancestry. Paused prepare-only protocol,
attempt-selection replacement, dirty runtime gate/release-config/auto-dispatch
changes and experimental docs stay in the existing development worktree.
Prior GATK concurrency/transfer fixes already exist on the base. The deferred
GATK backend-outage recovery is not implemented or deployed by this promotion.
No shared pending, analysis data, database reset or new batch submission.

## Verification

Exact candidate staged only in approved BS96 WGS_test/cce-evidence path.
Cached backend image, network none: existing test_wgs_file_reference.py,
test_wgs_file_history.py and test_gatk_config_versions.py:25 passed in5.96s.
Cached Node22 build, network none: tsc/vite1852 modules passed in3.07s.
Existing ResourceMonitoring.test.tsx:3 passed in9.55s on the same candidate.
No Docker Hub, full WGS replay or test matrix expansion. User independently
confirmed refreshed button appearance before this Git synchronization.

Both destination refs must point to the identical promotion commit after atomic
push; verify by git ls-remote. Preserve old commit29ade68 for code-only rollback.
No forced push, worktree deletion or data/schema downgrade.
