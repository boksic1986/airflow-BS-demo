# CCE 0.8.5 development delivery — not deployed

- CCE source: `b7f98e93e63fcf4872f3cfa963e910910eb864fc`, branch `jiucheng/cce-release-085`, baseline `ee2a815` (0.8.5rc1). Real Git remains on BS under `/bi/biodevrwbi/33.chenjiucheng/project/worktrees/huawei-cloud-runtime-bioinfo-permissions-083`.
- Airflow code: `994a9c2a0401a7e56071cff26af3e95e28c22cfe`, branch `jiucheng/backend/cce-release-085`, baseline `33598aa`; registration/activation and frozen CCE release contract only.
- Wheel: `cce_pipeline-0.8.5-py3-none-any.whl`.
- Wheel SHA256: `349ac3a00ded9ae11bf7e1b3aac501cf7284c98985fd7869b558991017cb8e76`.
- Local delivery: `D:/pipeline/task-artifacts/cce-release-20260915/dist/`.
- BS10610 evidence: `/mnt/biodevrwsg2/33.chenjiucheng/WGS_test/cce-evidence/cce-release-085-20260915`.

Only the approved four items were implemented: journal-owned Step6 permissions,
existing assets validate/apply/status orchestration, inactive registration and
explicit activation API, and preservation of retained assets/frozen CCE releases.
There are no WGS prepare/pending/local/SGE algorithm changes or runtime-gate edits.

Focused evidence: 14 CCE cases passed, 7 Airflow cases passed. Each review fix
reran only its existing covering case. The Airflow image reports one pre-existing
Starlette/anyio deprecation warning; dependencies were not changed.
One synthetic built-wheel producer/client to real FastAPI TestClient smoke passed
at CCE `b7f98e9` / Airflow `8fb3529`: registration inactive/idempotent, explicit
activation, historical entry and old CCE params/profile retained. Subsequent
Airflow `994a9c2` only rejects blank managed fields; its negative case passed and
scoped review confirmed the valid smoke remains applicable. No repeat integration,
full suite, matrix or clinical run was performed.

Wheel build used existing BS10610 WGS Python/setuptools without downloads; isolated
target installation verified 23 packaged source files byte-for-byte and five CLI
help entries. Production/shared WGS installation was not changed.

Final review approved both code commits. `WGS_RELEASE_MANAGEMENT_ENABLED` stays
false by default. No production install, live catalog update, activation, SFS
publication, cleanup, merge or push occurred. Future deployment still requires
the existing approval gates, a shared parent-directory catalog mount, correct
node200 wheel installation, and frozen asset/profile paths; see the release
management section in [deployment runbook](../11_DEPLOYMENT_RUNBOOK.md).
